# Как обновить сайт на GitHub Pages

## 🚀 Быстрая инструкция (3 шага)

Когда вы хотите изменить что-то на сайте:

### 1. Внесите изменения локально

Отредактируйте нужные файлы в вашем редакторе кода. Например:
- `App.tsx` — основная структура сайта
- `constants.ts` — данные (опыт работы, проекты, контакты)
- `components/` — отдельные компоненты
- `index.css` — стили

### 2. Проверьте изменения локально

Убедитесь, что dev-сервер запущен:
```bash
npm run dev
```

Откройте http://localhost:3000 и проверьте, что всё выглядит правильно.

### 3. Опубликуйте изменения

```bash
git add .
git commit -m "Описание изменений"
git push
```

**Готово!** Через 1-2 минуты изменения появятся на https://dun4ev.github.io/Dun4ev_site_r1/

---

## 📝 Подробная инструкция

### Что можно менять и где

#### 1. **Личная информация и контакты**
**Файл:** `constants.ts`

```typescript
export const SOCIAL_LINKS = {
  linkedin: 'https://linkedin.com/in/ваш-профиль',
  email: 'ваш@email.com',
  phone: '+381 XX XXX XXXX'
};
```

#### 2. **Опыт работы**
**Файл:** `constants.ts` → массив `JOBS`

```typescript
export const JOBS: Job[] = [
  {
    id: 1,
    company: 'Название компании',
    position: 'Должность',
    period: '2020 — Настоящее время',
    description: 'Описание работы...',
    technologies: ['Python', 'React', 'Docker'],
    link: 'https://company.com'
  },
  // ... добавьте больше записей
];
```

#### 3. **Проекты**
**Файл:** `constants.ts` → массив `PROJECTS`

```typescript
export const PROJECTS: Project[] = [
  {
    id: 1,
    title: 'Название проекта',
    description: 'Описание проекта...',
    technologies: ['Python', 'FastAPI'],
    link: 'https://github.com/username/repo',
    image: '/images/projects/project-name.png'
  },
  // ... добавьте больше проектов
];
```

#### 4. **Текст "О себе"**
**Файл:** `App.tsx` → секция `id="about"`

Найдите строки около 112-120 и отредактируйте текст в параграфах `<p>`.

#### 5. **Заголовок и подзаголовок**
**Файл:** `App.tsx`

```tsx
<h1>Ваше Имя</h1>
<TypingEffect text="Ваша должность" />
```

#### 6. **Изображения проектов**
Поместите изображения в папку `public/images/projects/` и укажите путь в `constants.ts`:
```typescript
image: '/images/projects/my-project.png'
```

#### 7. **Стили и цвета**
**Файл:** `index.css`

Можете изменить цветовую схему, шрифты и другие стили.

---

## 🔄 Полный рабочий процесс

### Пример: Изменение email

1. **Откройте файл** `constants.ts`

2. **Найдите и измените:**
   ```typescript
   export const SOCIAL_LINKS = {
     linkedin: 'https://linkedin.com/in/andrej-dunajev',
     email: 'новый@email.com',  // ← ИЗМЕНИЛИ ЭТО
     phone: '+381 XX XXX XXXX'
   };
   ```

3. **Сохраните файл** (Cmd+S / Ctrl+S)

4. **Проверьте локально:**
   - Откройте http://localhost:3000
   - Убедитесь, что новый email отображается

5. **Опубликуйте:**
   ```bash
   git add constants.ts
   git commit -m "Обновил email адрес"
   git push
   ```

6. **Дождитесь деплоя:**
   - Зайдите в https://github.com/Dun4ev/Dun4ev_site_r1/actions
   - Дождитесь зелёной галочки ✅ (1-2 минуты)
   - Обновите https://dun4ev.github.io/Dun4ev_site_r1/

---

## ⚡ Быстрые команды

### Проверить изменения локально
```bash
npm run dev
# Откройте http://localhost:3000
```

### Собрать production версию (для проверки)
```bash
npm run build
npm run preview
# Откройте http://localhost:4173
```

### Опубликовать изменения
```bash
git add .
git commit -m "Краткое описание изменений"
git push
```

### Проверить статус git
```bash
git status
```

### Посмотреть, что изменилось
```bash
git diff
```

---

## 🎯 Частые сценарии

### Добавить новый проект

1. Откройте `constants.ts`
2. Добавьте новый объект в массив `PROJECTS`:
   ```typescript
   {
     id: 4,  // следующий ID
     title: 'Новый проект',
     description: 'Описание нового проекта',
     technologies: ['Python', 'React'],
     link: 'https://github.com/username/project',
     image: '/images/projects/new-project.png'
   }
   ```
3. Добавьте изображение в `public/images/projects/new-project.png`
4. Сохраните, проверьте, запушьте

### Изменить цветовую схему

1. Откройте `index.css`
2. Найдите CSS переменные или классы с цветами
3. Измените нужные цвета
4. Сохраните, проверьте, запушьте

### Обновить резюме

1. Замените файл `public/resume.pdf` на новый
2. Запушьте изменения

---

## 🛠️ Устранение проблем

### Изменения не появляются на сайте?

1. **Проверьте статус деплоя:**
   - https://github.com/Dun4ev/Dun4ev_site_r1/actions
   - Должна быть зелёная галочка ✅

2. **Очистите кеш браузера:**
   - Cmd+Shift+R (Mac) или Ctrl+Shift+R (Windows)

3. **Подождите 2-3 минуты:**
   - GitHub Pages может кешировать контент

### Dev-сервер не запускается?

```bash
# Остановите текущий процесс (Ctrl+C)
# Переустановите зависимости
npm install
# Запустите снова
npm run dev
```

### Ошибка при git push?

```bash
# Проверьте статус
git status

# Если есть конфликты, посмотрите что изменилось
git diff

# Если нужно отменить изменения
git restore имя-файла
```

---

## 📋 Чек-лист перед публикацией

- [ ] Изменения внесены в нужные файлы
- [ ] Dev-сервер запущен (`npm run dev`)
- [ ] Проверил изменения на http://localhost:3000
- [ ] Всё выглядит правильно
- [ ] Выполнил `git add .`
- [ ] Выполнил `git commit -m "Описание"`
- [ ] Выполнил `git push`
- [ ] Проверил статус в Actions (зелёная галочка)
- [ ] Проверил сайт на https://dun4ev.github.io/Dun4ev_site_r1/

---

## 🎓 Полезные ссылки

- **Ваш сайт:** https://dun4ev.github.io/Dun4ev_site_r1/
- **Репозиторий:** https://github.com/Dun4ev/Dun4ev_site_r1
- **Actions (деплой):** https://github.com/Dun4ev/Dun4ev_site_r1/actions
- **Локальный dev:** http://localhost:3000

---

**Совет:** Делайте частые коммиты с понятными описаниями. Это поможет отслеживать изменения и при необходимости откатиться к предыдущей версии.
